# 📄 PDF Chunking and Question Dataset Generator

This project extracts text from PDFs, converts them into structured JSON chunks, and then uses those chunks to generate a question-answer dataset using OpenAI.

